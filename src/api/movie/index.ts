// import { defHttp } from '/@/utils/http/axios';

// enum Api {
//   MOVIE_LIST = '/v1/movies',
// }

// export const getMovieList = (params: any) => defHttp.get<any>({ url: Api.MOVIE_LIST, params });

// export const addMovie = (items: Array<any>) =>
//   defHttp.post({ url: Api.MOVIE_LIST, params: { items } });

// export const editCurrency = (id: any, params: any) =>
//   defHttp.put<any>({ url: Api.MOVIE_LIST + '/' + id, params });
